[project]
name = "log-cost-analyser"
version = "0.1.0"
description = "Analyse structured JSON logs for cost optimisation"
requires-python = ">3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.scripts]
log-cost-analyser = "log_cost_analyser.__main__:main"